plugins {
    id 'java-library'
    id 'maven-publish'
}

group 'com.mobile2b'
version '0.1.11-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
}

test {
    useJUnitPlatform()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'com.mobile2b'
            artifactId = 'm2b-webfunction-sdk'
            from components.java
        }
    }
}